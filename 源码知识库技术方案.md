# 源码知识库技术方案

> 定位：作为 OpenSpec + CodeGraph AI Coding 框架的第三层能力——将已有源码中的架构模式、设计决策、业务规则提取为结构化知识，并支持自然语言问答检索，使 AI 在后续编码中具备"项目记忆"。

---

## 一、问题分析与目标

### 1.1 现有框架能力边界

当前框架由两个核心组件构成：

**OpenSpec** 解决的是"做什么"的问题——通过规范驱动开发（Spec-Driven Development），在 AI 编码前先达成设计共识。它管理的是 specs/（当前系统规范）和 changes/（增量变更提案），本质上是一个规划层，不涉及代码的检索与理解。

**CodeGraph** 解决的是"代码结构是什么"的问题——基于 tree-sitter 的 AST 解析构建符号知识图谱，提供 callers、callees、trace、impact 等结构化查询，响应在亚毫秒级。它理解的是代码的骨架（谁调用谁、改了什么会崩），但不理解代码的语义（为什么这样设计、业务规则是什么）。

两者之间存在一个关键缺口：**语义知识层**。当 AI 面对一个已有项目时，它知道函数 A 调用了函数 B（CodeGraph），也知道要新增一个功能 C（OpenSpec），但它不知道"这个模块之所以用事件驱动而不是直接调用，是因为上游服务有 3 秒延迟"，不知道"所有金额计算必须用 BigDecimal 是财务合规要求"，不知道"这个看起来冗余的中间层是为了兼容 v1 API 的客户端"。

### 1.2 目标定义

源码知识库（以下称 **CodeKB**）需要填补这个缺口，具体目标：

**结构化知识提取**：从源码、注释、Git 历史、文档中提取架构模式、设计决策、业务规则、编码约定，形成可被 AI 在编码时自动引用的知识条目。

**自然语言问答**：支持开发者用自然语言提问，如"这个模块为什么用事件驱动？"、"用户鉴权的完整流程是什么？"、"X 和 Y 之间有什么依赖关系？"，返回基于项目实际代码的准确回答。

**与现有框架深度集成**：以 MCP Server 形式暴露工具供 AI Agent 按需调用；同时嵌入 OpenSpec 工作流，在 `/opsx:explore` 和 `/opsx:propose` 阶段自动注入相关知识上下文。

**混合部署**：核心索引（AST 图谱、向量索引、BM25 索引）全部本地存储和计算；知识提取和问答生成允许调用云端 LLM。

---

## 二、整体架构

### 2.1 分层架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                        集成层 (Integration)                         │
│  ┌──────────────────────┐    ┌──────────────────────────────────┐   │
│  │    MCP Server        │    │    OpenSpec 插件                  │   │
│  │  codekb_search       │    │  /opsx:explore 注入知识上下文      │   │
│  │  codekb_ask          │    │  /opsx:propose 关联设计决策        │   │
│  │  codekb_explain      │    │  spec 文件引用知识条目             │   │
│  │  codekb_conventions  │    │                                  │   │
│  └──────────┬───────────┘    └──────────────┬───────────────────┘   │
│             │                               │                       │
├─────────────┴───────────────────────────────┴───────────────────────┤
│                        检索层 (Retrieval)                           │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                   Query Router / Fusion                      │    │
│  │  查询分类 → 路由分发 → RRF 融合 → Cross-Encoder 重排序        │    │
│  └──────┬──────────────────┬──────────────────┬────────────────┘    │
│         │                  │                  │                     │
│  ┌──────┴──────┐   ┌──────┴──────┐   ┌──────┴──────┐              │
│  │  结构化检索   │   │  语义检索    │   │  词法检索    │              │
│  │  CodeGraph   │   │  Vector ANN │   │  BM25/FTS   │              │
│  │  符号图谱     │   │  (LanceDB)  │   │  (Tantivy)  │              │
│  └──────┬──────┘   └──────┬──────┘   └──────┬──────┘              │
│         │                  │                  │                     │
├─────────┴──────────────────┴──────────────────┴─────────────────────┤
│                        索引层 (Indexing)                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│  │  AST 解析     │  │  代码分块     │  │  知识提取 (LLM-powered)  │  │
│  │  tree-sitter │  │  AST-aware   │  │  架构模式 / 设计决策      │  │
│  │  158 种语言   │  │  chunking    │  │  业务规则 / 编码约定      │  │
│  │              │  │              │  │  缺陷模式               │  │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘  │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                        存储层 (Storage) — 全部本地                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│  │  图谱存储     │  │  向量存储     │  │  知识条目存储              │  │
│  │  SQLite /    │  │  LanceDB     │  │  Markdown + YAML         │  │
│  │  CodeGraph   │  │  (Lance格式)  │  │  (Git 版本管理)           │  │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 与现有框架的关系

CodeKB 不是 CodeGraph 的替代品，而是在其之上构建的语义层。三者的协作关系：

```
OpenSpec（规划层）──→ 定义"要做什么"，引用知识库中的设计约束
     │
     ▼
CodeKB（语义层）──→ 理解"为什么这样做"，提供知识上下文给规划和编码
     │
     ▼
CodeGraph（结构层）──→ 知道"代码长什么样"，提供精确的结构导航
```

在实际 AI 编码会话中，三者的典型协作流程是：AI 先通过 OpenSpec 明确任务规范，然后通过 CodeKB 获取相关的设计决策和业务规则（"这个模块有金额精度要求，必须用 BigDecimal"），最后通过 CodeGraph 定位具体要修改的符号和影响范围。

---

## 三、索引层设计

### 3.1 AST 结构化索引（复用 CodeGraph）

CodeKB 不重复构建 AST 索引，而是直接复用 CodeGraph 已有的 tree-sitter 解析结果。CodeGraph 已经提供了符号定义、调用关系、文件依赖等结构化数据，CodeKB 在此基础上增加语义标注。

具体做法是在 CodeGraph 的符号节点上挂载额外的元数据：

```yaml
# 符号节点增强示例
symbol: "PaymentService.processRefund"
codegraph_data:
  callers: ["RefundController.handle", "BatchJob nightlySettlement"]
  callees: ["LedgerService.reverse", "NotificationService.send"]
  file: "src/services/payment.ts"
codekb_annotations:        # ← CodeKB 新增
  summary: "处理退款请求，校验原始交易状态后执行账务冲正并通知用户"
  business_rules:
    - "退款金额不得超过原交易金额"
    - "已结算交易需走 T+1 冲正流程"
  design_decisions:
    - "使用 Saga 模式而非分布式事务，因为下游通知服务允许最终一致性"
  conventions:
    - "所有金额字段使用 BigDecimal，禁止 float/double"
  tags: ["payment", "refund", "saga-pattern"]
```

### 3.2 代码分块与向量索引

**分块策略：AST-aware chunking**

不使用固定行数分块（会切断函数/类结构），而是基于 tree-sitter 的 AST 按语义单元分块：

| 分块粒度 | 内容 | 适用场景 |
|---------|------|---------|
| 函数/方法级 | 函数签名 + 完整函数体 + 直接调用的函数签名 | 精确的代码语义检索 |
| 类/模块级 | 类的完整定义（含所有方法签名）+ 模块导入/导出 | 理解模块职责 |
| 文件级摘要 | LLM 生成的文件功能摘要（非原始代码） | 高层语义匹配 |

关键设计决策：**同时嵌入原始代码和 LLM 生成的自然语言摘要**。原始代码的嵌入处理"用代码术语搜索"的场景（如搜索 "retry backoff exponential"），NL 摘要的嵌入处理"用自然语言搜索"的场景（如搜索"哪里处理了网络重试逻辑"）。这是 Greptile 验证过的有效模式——NL 摘要弥合了自然语言查询与代码词汇之间的鸿沟。

**向量数据库选型：LanceDB**

选择 LanceDB 作为本地向量存储，理由：

| 评估维度 | LanceDB | SQLite-vec | ChromaDB | Qdrant |
|---------|----------|------------|----------|--------|
| 部署模式 | 嵌入式，无服务进程 | 嵌入式 | 嵌入式/服务 | 通常需服务进程 |
| 混合检索 | 原生支持（Tantivy BM25 + 向量） | 需手动组合 FTS5 | 无原生 BM25 | 支持 |
| 存储格式 | Lance 列式，单文件 | SQLite 单文件 | 多文件 | 服务管理 |
| 增量更新 | 支持 | 支持 | 支持 | 支持 |
| SDK | Rust / Python / Node | Python / C | Python / JS | 多语言 |
| 适合场景 | **本地优先的代码知识库** | 极简场景 | 原型验证 | 需高级过滤的服务端 |

LanceDB 的核心优势是"嵌入式 + 原生混合检索"——不需要启动任何服务进程，数据库就是一个目录下的 Lance 文件，同时内置了 Tantivy 引擎提供 BM25 全文检索，可以在一次查询中同时执行向量 ANN 搜索和 BM25 关键词搜索，天然适合混合检索架构。

**Embedding 模型选型**

采用双模型策略，本地优先、云端增强：

| 场景 | 模型 | 维度 | 上下文 | 部署 |
|------|------|------|--------|------|
| 本地默认 | jina-code-embeddings-0.5B | 896 | 32K tokens | 本地推理（ONNX/llama.cpp） |
| 云端增强 | voyage-code-3 | 1024（可变） | 32K tokens | API 调用 |
| 备选开源 | BGE-Code-v1 (BAAI) | 1536 | 32K tokens | 本地推理 |

jina-code-embeddings-0.5B 是当前开源代码嵌入模型中性价比最高的选择——基于 Qwen2.5-Coder 微调，在 MTEB 代码检索基准上达到 78.4%，仅比 1.5B 版本低 0.6 个百分点，比商业模型 voyage-code-3 低约 0.8 个百分点，但模型体积小到可以在消费级硬件上本地运行。Apache 2.0 许可证，无商用限制。

对于混合部署模式，建议默认使用 jina-code-0.5B 本地推理（保证代码不出本机），在用户显式配置后允许切换到 voyage-code-3 API 以获得最佳检索质量。

### 3.3 BM25 词法索引

BM25 索引由 LanceDB 内置的 Tantivy 引擎提供，无需额外组件。索引内容包括：代码原文、注释、文档字符串、Git commit message。BM25 在精确符号名、错误信息、字符串字面量检索上不可替代——当用户搜索 "NullPointerException in OrderService" 时，BM25 的精确匹配能力远超向量检索。

### 3.4 知识提取管线（LLM-powered）

这是 CodeKB 区别于纯检索工具的核心能力。通过 LLM 从多种来源提取结构化知识：

```
┌─────────────────────────────────────────────────────────────┐
│                    知识提取管线                               │
│                                                             │
│  输入源                    提取器                  输出       │
│  ┌──────────┐         ┌──────────────┐      ┌──────────┐   │
│  │ 源码+注释 │────────→│ 架构模式识别  │─────→│ 知识条目  │   │
│  └──────────┘         │ (Pattern     │      │ (YAML)   │   │
│  ┌──────────┐         │  Detector)   │      └──────────┘   │
│  │ Git 历史  │────────→├──────────────┤           │         │
│  │ + PR 描述 │         │ 设计决策提取  │           ▼         │
│  └──────────┘         │ (Decision    │      ┌──────────┐   │
│  ┌──────────┐         │  Extractor)  │      │ 知识图谱  │   │
│  │ README / │────────→├──────────────┤      │ (关联到   │   │
│  │ 设计文档  │         │ 业务规则提取  │      │ CodeGraph │   │
│  └──────────┘         │ (Rule        │      │ 符号节点) │   │
│  ┌──────────┐         │  Extractor)  │      └──────────┘   │
│  │ 测试用例  │────────→├──────────────┤                     │
│  │          │         │ 编码约定提取  │                     │
│  └──────────┘         │ (Convention  │                     │
│                       │  Extractor)  │                     │
│                       └──────────────┘                     │
└─────────────────────────────────────────────────────────────┘
```

**五类知识提取器：**

**架构模式识别器（Pattern Detector）**：分析模块间的依赖关系和通信方式，识别常见架构模式（事件驱动、CQRS、Saga、分层架构、六边形架构等）。输入是 CodeGraph 的模块依赖图 + 关键文件的源码，输出是模式标注和适用说明。

**设计决策提取器（Decision Extractor）**：从 Git commit message、PR 描述、代码注释（尤其是 `// WHY:` 、`// NOTE:` 、`// HACK:` 类注释）中提取设计决策及其理由。这类知识通常散落在历史中，是新人最难获取的。输出格式参照轻量级 ADR（Architecture Decision Record）。

**业务规则提取器（Rule Extractor）**：从条件分支、校验逻辑、常量定义、测试用例中提取隐含的业务规则。例如 `if (amount.compareTo(MAX_SINGLE_TRANSACTION) > 0) throw ...` 背后是"单笔交易限额"的业务规则。测试用例是特别好的输入源——每个测试的 given/when/then 结构天然描述了业务规则。

**编码约定提取器（Convention Extractor）**：从代码风格一致性中推断项目约定，如命名规范、错误处理模式、日志规范、依赖注入方式等。结合 `.editorconfig`、`eslint`/`prettier` 配置、CI 检查规则进行交叉验证。

**缺陷模式提取器（Bug Pattern Extractor）**：从 Git 历史中的 fix/bugfix/hotfix commit 提取缺陷模式。输入是 `git log --grep="fix" --grep="bug" --grep="hotfix"` 筛选的 commit 及对应 diff + 关联的 issue/PR 描述。输出包括：缺陷模式描述（如"并发下 HashMap 非线程安全导致数据丢失"）、触发条件列表、修复模式（如"改用 ConcurrentHashMap"）、涉及符号、防御性编码建议。同一符号的多次 fix commit 聚合为一条缺陷模式，`source_commits` 字段记录完整修复历史。

**提取策略：增量 + 按需**

全量提取仅在首次索引时执行。后续采用增量策略：通过 Git diff 检测变更文件，仅对变更涉及的符号重新提取知识。同时支持按需提取——当 AI 在编码过程中遇到 CodeKB 中尚无知识标注的符号时，触发实时提取（使用云端 LLM），结果缓存到本地。

### 3.5 知识条目存储格式

知识条目以 Markdown + YAML frontmatter 格式存储在项目的 `codekb/` 目录中，纳入 Git 版本管理：

```
project-root/
├── codekb/
│   ├── codekb.yaml              # 全局配置
│   ├── knowledge/
│   │   ├── architecture/
│   │   │   ├── event-driven-messaging.md
│   │   │   └── saga-payment-flow.md
│   │   ├── decisions/
│   │   │   ├── 001-why-bigdecimal.md
│   │   │   └── 002-auth-token-rotation.md
│   │   ├── rules/
│   │   │   ├── payment-limits.md
│   │   │   └── user-data-retention.md
│   │   ├── conventions/
│   │   │   ├── error-handling.md
│   │   │   └── naming.md
│   │   └── bug-patterns/
│   │       ├── concurrent-hashmap.md
│   │       └── null-check-order.md
│   └── index/                   # 本地索引（.gitignore）
│       ├── vectors.lance/       # LanceDB 向量索引
│       └── cache/               # LLM 提取缓存
```

单个知识条目示例：

```markdown
---
id: decision-001
type: design-decision
title: 金额计算使用 BigDecimal 而非浮点数
status: accepted
date: 2024-03-15
related_symbols:
  - "PaymentService.*"
  - "LedgerService.*"
  - "AmountUtils"
related_specs:
  - "openspec/specs/payment-core.md"
tags: [payment, precision, compliance]
confidence: 0.95
source: git-history + code-analysis
---

## 决策

所有涉及金额的字段和计算必须使用 `BigDecimal`（Java）或 `decimal.js`（TypeScript），
禁止使用 `float`/`double`/`number` 类型。

## 背景

2024年2月发生过一次线上事故：浮点精度丢失导致对账差异累计达到 ¥127.43，
触发了财务合规审计。详见 commit `a3f29c1` 和 PR #1847。

## 影响

- 所有金额字段类型变更，涉及 23 个文件
- 数据库字段从 DOUBLE 改为 DECIMAL(19,4)
- 前端展示层增加 toFixed(2) 保护

## 关联知识

- [支付限额规则](../rules/payment-limits.md)
- [Saga 支付流程](../architecture/saga-payment-flow.md)
```

选择 Markdown + YAML 而非纯数据库存储的原因：知识条目需要被人类审阅和修正（LLM 提取不可能 100% 准确），Markdown 是最友好的可编辑格式；纳入 Git 管理后，知识的演变历史可追溯；OpenSpec 的 spec 文件也是 Markdown 格式，保持一致性。

---

## 四、检索层设计

### 4.1 三路混合检索

CodeKB 的检索层融合三种信号，覆盖不同类型的查询：

```
用户查询: "退款失败时系统怎么处理的？"
         │
         ▼
┌─────────────────────┐
│    Query Router      │  ← 轻量分类器（或规则）判断查询类型
│    查询路由           │
└──┬──────┬──────┬────┘
   │      │      │
   ▼      ▼      ▼
┌─────┐┌─────┐┌─────┐
│结构化││语义 ││词法 │
│检索  ││检索 ││检索 │
│     ││     ││     │
│Code ││Lance││Tant │
│Graph││DB   ││ivy  │
│     ││ANN  ││BM25 │
└──┬──┘└──┬──┘└──┬──┘
   │      │      │
   ▼      ▼      ▼
┌─────────────────────┐
│   RRF 融合排序       │  ← Reciprocal Rank Fusion
│   + Cross-Encoder   │  ← 可选：Jina Reranker / BGE Reranker
│   重排序             │
└──────────┬──────────┘
           ▼
     Top-K 结果 + 知识条目
```

**各检索通道的职责：**

| 查询类型 | 示例 | 主检索通道 | 辅助通道 |
|---------|------|-----------|---------|
| 精确符号查找 | "PaymentService 的 processRefund 方法" | 结构化（CodeGraph） | 词法 |
| 自然语言理解 | "哪里处理了用户鉴权逻辑？" | 语义（向量） | 词法 |
| 错误/字符串搜索 | "NullPointerException OrderService" | 词法（BM25） | 语义 |
| 影响分析 | "改了 AmountUtils 会影响什么？" | 结构化（CodeGraph impact） | — |
| 设计意图 | "为什么消息模块用事件驱动？" | 知识条目检索 | 语义 |
| 流程追踪 | "从下单到支付的完整调用链" | 结构化（CodeGraph trace） | 知识条目 |

**RRF（Reciprocal Rank Fusion）融合**：将三个检索通道各自的排序结果通过 RRF 公式合并，不需要对各通道的原始分数做归一化（BM25 分数和余弦相似度量纲不同，直接比较无意义）。RRF 公式：`score(d) = Σ 1/(k + rank_i(d))`，其中 k 通常取 60。

**可选重排序**：对于高精度要求的场景，在 RRF 融合后增加 Cross-Encoder 重排序阶段。推荐使用 jina-reranker-v2（开源，支持本地部署）或 BGE-reranker-v2-m3。重排序模型对候选集（通常 20-50 条）做精细打分，显著提升 Top-5 精度。

### 4.2 上下文组装

检索到相关代码片段和知识条目后，需要组装为适合 LLM 消费的上下文。关键原则是**有界、分层、去噪**：

```
组装后的上下文结构（控制在 4K tokens 以内）：

[知识层] 相关设计决策和业务规则（来自 codekb/knowledge/）
   ↓ 提供"为什么"的背景
[结构层] 相关符号的签名和调用关系（来自 CodeGraph）
   ↓ 提供"是什么"的骨架
[代码层] 最相关的代码片段（来自向量/BM25 检索）
   ↓ 提供"怎么做"的细节
```

Token 预算分配建议：知识层 ~800 tokens，结构层 ~1200 tokens，代码层 ~2000 tokens。这个分配参照了 Aider 的 repo-map 策略（约 1K tokens 的结构摘要）和 Cursor 的上下文窗口管理经验。

---

## 五、MCP Server 设计

### 5.1 工具定义

CodeKB MCP Server 暴露以下工具，与 CodeGraph 的工具形成互补：

```typescript
// === 知识检索类 ===

// 混合检索：自动路由到最合适的检索通道
codekb_search(query: string, options?: {
  scope?: string[]        // 限定搜索范围（目录/模块）
  knowledge_types?: string[]  // 过滤知识类型: decision | rule | convention | pattern
  max_results?: number    // 默认 10
}) → SearchResult[]

// 自然语言问答：基于项目代码的 RAG 问答
codekb_ask(question: string, options?: {
  context_symbols?: string[]  // 提供额外上下文（当前正在编辑的符号）
  include_sources?: boolean   // 是否返回引用来源
}) → Answer & { sources: Source[] }

// === 知识浏览类 ===

// 解释符号：返回符号的功能摘要 + 关联知识 + 调用关系
codekb_explain(symbol: string) → {
  summary: string
  knowledge: KnowledgeEntry[]
  known_issues: BugPatternEntry[]  // 关联的缺陷模式（按 severity 降序）
  callgraph: { callers: string[], callees: string[] }  // 复用 CodeGraph
  conventions: string[]  // 该符号涉及的编码约定
}

// 获取项目约定：返回全局或特定领域的编码约定
codekb_conventions(domain?: string) → Convention[]

// 列出知识条目：按类型/标签浏览知识库
codekb_knowledge(filter?: {
  type?: string
  tags?: string[]
  related_symbol?: string
  status?: string       // accepted | superseded | rejected | potentially_stale
}) → KnowledgeEntry[]

// === 知识管理类 ===

// 触发知识提取：对指定范围执行 LLM 知识提取
codekb_extract(scope: string, options?: {
  extractors?: string[]   // 指定提取器: pattern | decision | rule | convention | bug-pattern
  force?: boolean         // 强制重新提取（忽略缓存）
  from_change?: string    // 从 OpenSpec 变更中提取（archive 阶段使用）
  stale_only?: boolean    // 仅重新提取标记为 potentially_stale 的条目
}) → ExtractionReport

// 知识条目审阅：标记知识条目的准确性
codekb_review(id: string, verdict: "confirm" | "reject" | "edit", edit?: string) → void
```

### 5.2 与 CodeGraph MCP 的协作

CodeKB MCP Server 在内部调用 CodeGraph 的 API/工具获取结构数据，对外呈现为统一的语义增强结果。例如 `codekb_explain("PaymentService.processRefund")` 的返回：

```json
{
  "summary": "处理退款请求。校验原始交易状态（必须为 SETTLED 或 COMPLETED），通过 Saga 编排执行账务冲正和用户通知。",
  "knowledge": [
    {
      "type": "decision",
      "title": "使用 Saga 模式而非分布式事务",
      "reason": "下游通知服务允许最终一致性，Saga 的补偿机制比 2PC 更适合此场景"
    },
    {
      "type": "rule",
      "title": "退款金额不得超过原交易金额",
      "source": "RefundValidator + 测试用例 RefundServiceTest"
    }
  ],
  "callgraph": {
    "callers": ["RefundController.handle", "BatchJob.nightlySettlement"],
    "callees": ["LedgerService.reverse", "NotificationService.send", "RefundValidator.validate"]
  },
  "conventions": [
    "金额字段使用 BigDecimal",
    "Saga 步骤必须实现 compensate() 方法"
  ]
}
```

### 5.3 服务器实现技术栈

```
运行时:       Node.js (TypeScript) 或 Rust
MCP SDK:     @modelcontextprotocol/sdk (TypeScript) 或 rmcp (Rust)
AST 解析:    tree-sitter (通过 CodeGraph 复用，或直接调用 node-tree-sitter)
向量存储:    @lancedb/lancedb (Node SDK) 或 lancedb (Rust crate)
嵌入推理:    onnxruntime-node (本地) / voyageai SDK (云端)
BM25:        LanceDB 内置 Tantivy
LLM 调用:    OpenAI-compatible API (支持任意兼容端点)
配置:        codekb.yaml (项目级) + ~/.codekb/config.yaml (全局)
```

推荐 TypeScript 实现——MCP SDK 的 TypeScript 版本最成熟，tree-sitter 的 Node binding 稳定，LanceDB 的 Node SDK 功能完整，且与 OpenSpec（也是 npm 生态）保持一致。

---

## 六、初始化流程

### 6.1 初始化命令

```bash
# 安装（全局 CLI + MCP Server）
npm install -g @your-scope/codekb

# 在项目根目录初始化
cd your-project
codekb init
```

### 6.2 初始化执行流程

`codekb init` 按以下顺序执行四个阶段：

```
codekb init
     │
     ▼
┌──────────────────────────────────────────────────────────┐
│ 阶段 1：项目探测                                          │
│                                                          │
│  扫描目录结构，识别语言和框架                                │
│  (package.json / pom.xml / go.mod / Cargo.toml 等)       │
│       │                                                  │
│       ▼                                                  │
│  生成 codekb/codekb.yaml 默认配置                         │
│  (include/exclude 规则、语言列表、嵌入模型选择)              │
│       │                                                  │
│       ▼                                                  │
│  检测 .codegraph/ 是否存在                                │
│  ├─ 存在 → 继续                                          │
│  └─ 不存在 → 提示: "请先执行 codegraph init -i"            │
│              (不阻塞，但结构化检索通道将降级)                 │
└──────────────────────┬───────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ 阶段 2：全量代码索引                                      │
│                                                          │
│  对 include 范围内所有文件执行:                             │
│       │                                                  │
│       ├──→ tree-sitter AST 解析                          │
│       │         │                                        │
│       │         ├──→ AST-aware 分块                       │
│       │         │    (函数级 / 类级 / 文件摘要级)           │
│       │         │                                        │
│       │         └──→ 构建 Merkle Tree (内容哈希)           │
│       │                                                  │
│       ├──→ 批量向量嵌入                                   │
│       │    (jina-code-0.5B 本地 / voyage-code-3 云端)     │
│       │         │                                        │
│       │         └──→ 写入 codekb/index/vectors.lance/     │
│       │                                                  │
│       └──→ 构建 BM25 全文索引 (Tantivy)                   │
│            (代码原文 + 注释 + docstring + commit message)  │
│                                                          │
│  耗时参考: 5万行 ~1.5min(本地) / 10万行 ~3-5min(本地)      │
└──────────────────────┬───────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ 阶段 3：全量知识提取 (可选, --skip-extract 跳过)            │
│                                                          │
│  对每个模块/关键文件调用 LLM 提取四类知识:                   │
│       │                                                  │
│       ├──→ Pattern Detector  → knowledge/architecture/    │
│       ├──→ Decision Extractor → knowledge/decisions/      │
│       ├──→ Rule Extractor    → knowledge/rules/           │
│       └──→ Convention Extractor → knowledge/conventions/  │
│                                                          │
│  提取结果缓存到 codekb/index/cache/ (内容哈希去重)          │
│                                                          │
│  输出提取报告:                                             │
│  ┌────────────────────────────────────────────┐           │
│  │  知识提取报告                                │           │
│  │  架构模式: 3 条 (avg confidence: 0.88)      │           │
│  │  设计决策: 12 条 (avg confidence: 0.82)     │           │
│  │  业务规则: 7 条 (avg confidence: 0.91)      │           │
│  │  编码约定: 5 条 (avg confidence: 0.85)      │           │
│  │  ⚠ 待审阅 (confidence < 0.7): 3 条         │           │
│  │    - decisions/009-legacy-adapter.md (0.62) │           │
│  │    - rules/data-retention.md (0.58)         │           │
│  │    - conventions/logging.md (0.65)          │           │
│  └────────────────────────────────────────────┘           │
└──────────────────────┬───────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ 阶段 4：环境配置                                          │
│                                                          │
│  生成 .gitignore 条目:                                    │
│    codekb/index/        # 索引可重建，不入库               │
│    (codekb/knowledge/ 保留在 Git 中)                      │
│                                                          │
│  安装 Git post-commit hook:                               │
│    .git/hooks/post-commit → codekb sync --incremental     │
│                                                          │
│  输出 MCP Server 配置片段:                                 │
│  {                                                       │
│    "mcpServers": {                                       │
│      "codekb": {                                         │
│        "command": "codekb",                              │
│        "args": ["mcp"],                                  │
│        "env": { "CODEKB_PROJECT": "<project-path>" }     │
│      }                                                   │
│    }                                                     │
│  }                                                       │
│                                                          │
│  输出初始化摘要:                                           │
│  ✓ 索引文件: 247 个 | 代码行: 52,340 | 分块: 3,891       │
│  ✓ 知识条目: 27 条 (3 条待审阅)                           │
│  ✓ Git hook 已安装                                       │
│  → 请将上述 MCP 配置添加到你的 AI 编码工具中                │
└──────────────────────────────────────────────────────────┘
```

### 6.3 初始化后的目录状态

```
your-project/
├── .codegraph/              # CodeGraph 索引（已有）
├── .git/hooks/post-commit   # CodeKB 增量同步 hook（init 安装）
├── codekb/
│   ├── codekb.yaml          # 配置（init 自动生成，可手动调整）
│   ├── knowledge/           # 知识条目（Git 管理）
│   │   ├── architecture/    # 架构模式 (3-8 条)
│   │   ├── decisions/       # 设计决策 (10-30 条)
│   │   ├── rules/           # 业务规则 (5-20 条)
│   │   └── conventions/     # 编码约定 (3-10 条)
│   └── index/               # 本地索引（.gitignore）
│       ├── vectors.lance/   # LanceDB 向量 + BM25 索引
│       ├── merkle.json      # Merkle Tree 哈希
│       └── cache/           # LLM 提取缓存
├── openspec/
└── src/
```

### 6.4 幂等性保证

重复执行 `codekb init` 不破坏已有知识库：配置文件已存在时跳过生成（提示 `--force` 覆盖），`knowledge/` 目录中的已有条目不被覆盖（仅补充缺失的），索引目录全量重建。

---

## 七、OpenSpec 集成设计

### 7.1 各阶段数据流总览

```
阶段            CodeKB 角色      触发方式           写入内容
──────────────────────────────────────────────────────────────────
explore         被读取          AI 按需调用 MCP     临时缓存（不入知识库）
propose         被读取+关联     AI 按需调用 MCP     元数据（referenced_by）
apply           增量同步        Git hook 自动       向量索引 + BM25 + 过期标记
archive         知识沉淀        archive 后触发      新增/更新知识条目
──────────────────────────────────────────────────────────────────
日常 commit     增量同步        Git hook 自动       向量索引 + BM25 + 过期标记
定期维护        知识刷新        手动/CI 触发        重新提取 stale 条目
```

### 7.2 `/opsx:explore` 阶段——只读消费

这个阶段 AI 在理解现有系统，CodeKB 是被消费的一方，不产生知识库写入。

```
AI 执行 /opsx:explore
       │
       ├──→ codekb_search("项目整体架构")
       │      → 返回架构模式条目 + 模块概览
       │
       ├──→ codekb_conventions()
       │      → 返回全局编码约定
       │
       ├──→ codekb_explain("<核心模块名>")
       │      → 返回功能摘要 + 关联知识 + 调用关系
       │
       └──→ 遇到无知识标注的符号?
              ├─ 是 → 触发按需实时提取 (云端 LLM)
              │       → 结果写入 codekb/index/cache/ (临时缓存)
              │       → 不写入 knowledge/ (避免低质量条目)
              └─ 否 → 直接返回已有知识
```

AI 的 explore 输出中自动包含知识概览：

```
"这个项目使用了事件驱动架构（3 个核心事件总线），
 包含 12 条设计决策、7 条业务规则、5 条编码约定。
 关键约束：金额必须用 BigDecimal (decision-001)，
 跨服务操作走 Saga (architecture/saga-payment-flow)。"
```

### 7.3 `/opsx:propose` 阶段——约束关联

AI 撰写变更提案时，CodeKB 提供约束检查并记录关联关系。

```
AI 执行 /opsx:propose "增加退款手续费功能"
       │
       ├──→ codekb_search("退款 设计约束",
       │      knowledge_types: ["decision", "rule"])
       │      → 返回相关的设计决策和业务规则
       │
       ├──→ 解析 spec 文件中的 codekb:ref 引用
       │      → 收集所有被引用的知识条目
       │
       ├──→ 约束检查:
       │      提案内容 vs 已有约束 → 冲突检测
       │      (如提案中使用了 float 类型 → 违反 decision-001)
       │
       └──→ 写入元数据:
              在相关条目的 frontmatter 中追加:
              referenced_by:
                - change: add-refund-fee
                  date: 2026-08-02
```

提案中自动附带的约束部分：

```markdown
<!-- openspec/changes/add-refund-fee/proposal.md -->
## 相关约束（由 CodeKB 自动关联）

本变更涉及支付模块，需遵守以下已有约束：
- [decision-001] 金额计算使用 BigDecimal，禁止浮点类型
- [rule/payment-limits] 单笔交易限额 ¥50,000
- [architecture/saga-payment-flow] 跨服务操作使用 Saga 编排
- [convention/error-handling] 业务异常使用 BizException 体系
```

仅关联 confidence ≥ 0.7 的条目，避免不准确的知识误导设计决策。

### 7.4 `/opsx:apply` 阶段——代码变更触发增量索引

这是代码实际被修改的阶段，CodeKB 的更新由 Git hook 自动驱动。

```
AI 编码 → 开发者 commit
       │
       ▼
.git/hooks/post-commit 触发
       │
       ▼
codekb sync --incremental
       │
       ├──→ git diff --name-only HEAD~1
       │      → [src/services/payment.ts, src/utils/amount.ts]
       │
       ├──→ 代码索引更新 (自动, 确定性):
       │      tree-sitter 增量解析变更文件
       │      → 重新分块变更的函数/类
       │      → 重新计算 embedding
       │      → upsert LanceDB 向量记录 (by file+symbol)
       │      → 更新 BM25 索引
       │      → 更新 Merkle Tree
       │
       └──→ 知识影响评估 (标记, 不改写):
              检查变更符号是否关联已有知识条目
              → amount.ts 变更涉及 decision-001 的 related_symbols
              → 标记 decision-001 为 potentially_stale
              → 不自动修改知识内容 (LLM 推理可能误判)
              → 下次 codekb extract --stale 时优先重新提取
```

关键设计决策：**代码索引自动实时更新，知识条目仅标记过期不自动改写**。原因是代码 embedding 是确定性计算（同样的代码一定产生同样的向量），而知识提取涉及 LLM 推理，自动改写可能引入错误。

AI 在 apply 阶段也可主动查询：

```
AI 调用 codekb_ask("这个项目的错误处理模式是什么？")
  → RAG 问答，返回基于项目代码的回答

AI 调用 codekb_conventions("payment")
  → 获取支付模块编码约定，确保生成代码符合项目风格
```

### 7.5 `/opsx:archive` 阶段——知识反哺（最重要的更新点）

archive 意味着变更已完成并经过验证，此时产生的知识是高质量的。这是知识从"变更"沉淀为"共识"的关键时刻。

```
/opsx:archive 完成
       │
       ▼
检测 auto_extract_on_archive 配置
       │
       ├─ false → 跳过，仅输出提示
       │
       └─ true (默认) → 触发知识提取
              │
              ▼
       codekb extract --from-change <change-name>
              │
              ├──→ 读取 openspec/changes/<name>/ 下的文件:
              │      proposal.md    → 提取设计决策
              │                     ("为什么选方案A不选方案B")
              │      spec-delta.md  → 提取新增/修改的业务规则
              │      archive.md     → 提取实施经验教训
              │
              ├──→ 获取本次变更涉及的代码文件 (git log):
              │      → 提取新的编码约定 (如果引入了新模式)
              │      → 更新已有知识条目 (如果变更推翻了旧决策)
              │
              ├──→ 输出新知识条目:
              │      codekb/knowledge/decisions/007-refund-async.md
              │      (source_change: add-refund-fee ← 溯源)
              │
              ├──→ 处理取代关系:
              │      新决策取代旧决策?
              │      → 旧条目 status: accepted → superseded
              │      → 旧条目追加 superseded_by: decision-007
              │      → 新条目追加 supersedes: decision-003
              │
              └──→ 确认 codekb:ref 引用有效性:
                     spec 中引用了 decision-001?
                     → 本次变更未违反 → 引用有效，无需操作
                     → 本次变更修改了相关逻辑 → 触发 decision-001 重新提取
```

archive 提取生成的知识条目示例：

```markdown
# codekb/knowledge/decisions/007-refund-async-queue.md
---
id: decision-007
type: design-decision
title: 退款处理改为异步队列
status: accepted
date: 2026-08-02
source_change: add-refund-fee     # ← 溯源到 OpenSpec 变更
related_symbols:
  - "RefundQueueProcessor.*"
  - "RefundController.handle"
supersedes: decision-003           # ← 取代了哪条旧决策
confidence: 0.92
---

## 决策
退款请求不再同步处理，改为投递到消息队列异步消费。

## 理由
（从 proposal.md 和 archive.md 中提取）
同步退款在高峰期 P99 延迟达到 8s，改为异步后降至 200ms...
```

### 7.6 Spec 文件中的知识引用

在 OpenSpec 的 spec 文件中支持引用 CodeKB 知识条目：

```markdown
<!-- openspec/specs/payment-core.md -->
# 支付核心模块

## 概述
处理交易、退款、对账的核心服务。

## 约束
<!-- codekb:ref decision-001 -->
所有金额计算使用 BigDecimal，禁止浮点类型。

<!-- codekb:ref rule/payment-limits -->
单笔交易限额 ¥50,000，单日累计限额 ¥200,000。

<!-- codekb:ref architecture/saga-payment-flow -->
跨服务操作使用 Saga 编排，不使用分布式事务。
```

这些引用在 `/opsx:propose` 阶段被自动解析，确保变更提案不违反已有约束。引用使用 HTML 注释语法，不影响 Markdown 渲染。

### 7.7 日常维护（非 OpenSpec 流程）

不走 OpenSpec 流程的普通 commit，Git hook 的增量同步保证代码索引始终最新。知识条目的定期刷新通过以下方式触发：

```bash
# 手动刷新所有标记为 potentially_stale 的条目
codekb extract --stale

# 在 CI 中定期触发（如每周）
codekb extract --stale --ci

# 查看当前有多少过期条目
codekb status
# → 索引健康: ✓ | 待同步文件: 0 | 过期知识条目: 3
```

### 7.8 知识生命周期闭环

```
OpenSpec 管理设计意图
       │
       ▼ (archive 触发提取)
CodeKB 提取并存储知识
       │
       ▼ (explore/propose 注入)
知识反哺下一次 OpenSpec 规划
       │
       ▼ (apply 编码时引用)
AI 生成符合项目约定的代码
       │
       ▼ (commit 触发增量同步)
代码索引更新 + 知识过期标记
       │
       ▼ (定期 extract --stale)
知识刷新 → 回到起点
```

---

## 八、增量索引与性能

### 8.1 增量更新策略

```
┌─────────────────────────────────────────────────────┐
│                  增量索引管线                         │
│                                                     │
│  触发方式:                                           │
│  1. Git hook (post-commit / post-checkout)           │
│  2. 文件监听 (chokidar / notify)                     │
│  3. 手动触发 (codekb sync)                           │
│                                                     │
│  流程:                                               │
│  git diff --name-only HEAD~1                        │
│       │                                             │
│       ▼                                             │
│  变更文件列表                                        │
│       │                                             │
│       ├──→ tree-sitter 增量解析 (仅变更文件)          │
│       │         │                                   │
│       │         ├──→ 更新 CodeGraph 符号图谱          │
│       │         └──→ 重新分块 + 重新嵌入              │
│       │                   │                         │
│       │                   └──→ 更新 LanceDB 索引     │
│       │                                             │
│       └──→ 知识影响评估 (仅标记, 不改写)              │
│                 │                                   │
│                 ├──→ 检查变更符号关联的知识条目         │
│                 └──→ 标记为 potentially_stale         │
│                      (下次 extract --stale 时重新提取) │
│                                                     │
│  Merkle Tree 校验:                                   │
│  对每个文件计算内容哈希，构建 Merkle Tree，             │
│  通过根哈希快速判断索引是否需要更新                     │
│  (参照 Cursor 的增量索引方案)                         │
└─────────────────────────────────────────────────────┘
```

### 8.2 性能目标

| 操作 | 目标延迟 | 实现方式 |
|------|---------|---------|
| 结构化查询（符号/调用关系） | < 5ms | CodeGraph 内存图谱 |
| BM25 关键词检索 | < 50ms | Tantivy 倒排索引 |
| 向量 ANN 检索（10万条） | < 100ms | LanceDB IVF-PQ 索引 |
| 混合检索 + RRF 融合 | < 200ms | 三路并行 + 融合 |
| 知识条目检索 | < 30ms | YAML 索引 + 内存缓存 |
| 首次全量索引（10万行） | < 5min | 并行分块 + 批量嵌入 |
| 增量索引（单次 commit） | < 5s | 仅处理变更文件 |
| LLM 知识提取（单符号） | < 10s | 云端 LLM API |

### 8.3 大规模代码库适配

针对 50 万行以上的大型/微服务代码库：

**分仓库索引**：每个仓库独立维护 LanceDB 索引和知识条目，通过全局配置文件 `~/.codekb/config.yaml` 注册多个仓库，支持跨仓库联合检索。

**模块级懒加载**：不一次性索引整个代码库，而是按模块/目录粒度懒加载。AI 在编码时触及某个模块时才触发该模块的索引构建。

**嵌入批处理**：大规模首次索引时，使用 ONNX Runtime 的批量推理（batch size 32-64），或切换到云端 Embedding API 并行处理。

---

## 九、分支感知设计

### 9.1 问题分析

当前方案中，知识条目（`codekb/knowledge/`）是 Git 管理的文件，向量索引（`codekb/index/`）是 gitignore 的本地数据。这在多分支协作下产生三个问题：索引与分支不同步（切换分支后索引过期）、知识条目分叉（两个分支各自新增条目导致 merge 冲突）、临时知识污染（废弃分支上的实验性条目成为噪声）。

### 9.2 分支类型策略

```
分支类型          索引策略                    知识策略
──────────────────────────────────────────────────────────────────
main/master      完整索引，始终最新            权威知识库，条目 status=accepted
feature/*        增量索引（post-checkout 触发） 新条目 status=draft，仅本分支可见
hotfix/*         增量索引，只读查询模式         不触发提取，合入 main 后沉淀
release/*        冻结索引（只读）              冻结知识（不触发提取）
```

### 9.3 索引同步机制

`codekb init` 时安装两个 Git hook：

```bash
# .git/hooks/post-commit（已有）
#!/bin/sh
codekb sync --incremental

# .git/hooks/post-checkout（新增）
#!/bin/sh
# $3 = 1 表示分支切换（而非文件 checkout）
if [ "$3" = "1" ]; then
  codekb sync --incremental
fi
```

分支切换后的增量同步流程：Merkle Tree 对比当前工作区与索引记录的哈希差异，仅重新处理变更文件。典型分支切换（10-30 个文件差异）的增量同步在 2-3 秒内完成。若差异超过总文件数的 50%（如从长期 feature 分支切回 main），自动降级为全量重建。

### 9.4 知识条目分支感知

知识条目 frontmatter 增加分支相关字段：

```yaml
---
id: decision-008
type: design-decision
title: 使用 gRPC 替代 REST 进行内部通信
status: draft                    # feature 分支上产生 → draft
branch_origin: feature/grpc-migration   # 产生该条目的分支
promoted_to_main: false          # 合入 main 后改为 true
confidence: 0.85
---
```

**生命周期规则：**

- feature 分支上 `codekb extract` 产生的条目：`status: draft`，`branch_origin: <当前分支>`
- draft 条目仅在产生它的分支上参与检索，不污染其他分支
- feature 合入 main 后（merge 或 rebase），触发 `codekb promote`：draft → accepted，`promoted_to_main: true`
- 分支被删除（`git branch -d`）时，其独有的 draft 条目自然消失（因为只存在于该分支的 Git 历史中）

**检索时的分支过滤：**

```
codekb_search 默认行为:
  返回 = (main 上 status=accepted 的条目)
       + (当前分支上 status=draft 的条目)
       + (当前分支上代码的向量/BM25 检索结果)

可选参数 include_drafts: false → 排除 draft 条目
```

### 9.5 合并冲突处理

当 `git merge` 产生 `codekb/knowledge/` 下的冲突时：

```bash
codekb merge-resolve
```

执行流程：

```
扫描 codekb/knowledge/ 下的 Git 冲突标记 (<<<<<<)
       │
       ├──→ 简单冲突（不同位置各自新增条目）
       │      → 自动合并（保留双方新增）
       │      → 检查 ID 是否重复，重复则重新编号
       │
       ├──→ 语义冲突（同一主题的不同决策）
       │      → 调用 LLM 分析两条决策是否矛盾
       │      → 矛盾 → 输出合并建议，等待人工选择
       │      → 互补 → 自动合并为一条更完整的条目
       │
       └──→ 取代冲突（一方 supersede 了另一方的条目）
              → 自动应用取代关系
              → 被取代条目 status → superseded
```

### 9.6 LanceDB 索引分支策略

两种模式，通过 `codekb.yaml` 配置：

```yaml
# 默认：单索引 + 增量更新
index:
  branch_mode: single        # 切换分支时 sync，2-3s 延迟

# 大型项目可选：多索引快照
# index:
#   branch_mode: snapshot     # 每个活跃分支独立索引表
#   max_snapshots: 5          # 最多保留 5 个分支快照（LRU 淘汰）
```

单索引模式：所有分支共享一个 LanceDB 目录，切换分支时增量更新。简单、省磁盘，适合大多数项目。

多索引快照模式：利用 LanceDB 的表版本能力，为每个活跃分支维护独立的索引表。切换分支时直接切换表引用（零延迟），但每个快照占用完整磁盘空间。适合 50 万行以上且频繁切换分支的大型项目。`max_snapshots` 控制磁盘占用上限，超出时 LRU 淘汰最久未使用的快照。

---

## 十、Bugfix 辅助设计

### 10.1 缺陷模式知识条目

Bug Pattern Extractor 输出的知识条目格式：

```markdown
---
id: bugpattern-003
type: bug-pattern
title: 并发场景下共享 HashMap 导致数据丢失
status: accepted
severity: high
date: 2025-11-20
trigger_conditions:
  - "多线程环境（Web 容器 / 线程池）"
  - "共享可变 Map 无同步保护"
  - "高并发写入（QPS > 100）"
fix_pattern: "改用 ConcurrentHashMap 或 Collections.synchronizedMap"
related_symbols:
  - "SessionCache.*"
  - "RequestContext.attributes"
related_conventions:
  - "convention/thread-safety"
source_commits: ["a3f29c1", "b7e44d2", "c9f12e8"]
tags: [concurrency, thread-safety, data-loss]
confidence: 0.93
---

## 缺陷模式

多线程环境下对共享 HashMap 的并发写入导致内部链表/红黑树结构损坏，
表现为数据偶发丢失（get 返回 null）或死循环（CPU 100%）。

## 触发条件

- Web 容器中多个请求线程同时操作同一个 Map 实例
- 该 Map 被声明为类字段（非方法局部变量）
- 无 synchronized / Lock / Concurrent 保护

## 修复模式

将 `HashMap` 替换为 `ConcurrentHashMap`。若需要复合操作原子性
（如 check-then-act），使用 `ConcurrentHashMap.computeIfAbsent()`
而非外部加锁。

## 防御性编码建议

- 所有类级别 Map/Set 字段默认使用 Concurrent 版本
- Code Review 时关注 `new HashMap<>()` 出现在类字段声明中的情况
- 单元测试中增加并发压力测试（多线程同时读写）

## 修复历史

- `a3f29c1` (2025-06): SessionCache 首次修复
- `b7e44d2` (2025-09): RequestContext 同类问题
- `c9f12e8` (2025-11): 增加 convention/thread-safety 约定
```

### 10.2 Bugfix 工作流中的 CodeKB 使用

AI 在 bugfix 场景中的典型调用链：

```
收到 bug: "用户会话数据偶发丢失"
       │
       ├──→ codekb_search("会话数据丢失 并发")
       │      → 命中 bugpattern-003 (bug-pattern, 高权重)
       │      → 命中 decision-012 ("SessionCache 设计为单例")
       │
       ├──→ codekb_explain("SessionCache")
       │      → summary: "全局会话缓存，单例模式"
       │      → known_issues: [bugpattern-003]    ← 新增字段
       │      → callgraph: 被 8 个 Controller 并发调用
       │      → conventions: ["convention/thread-safety"]
       │
       ├──→ codekb_ask("SessionCache 的线程安全约定是什么？")
       │      → "convention/thread-safety 要求共享状态必须用线程安全容器。
       │         SessionCache.attributes 是已知遗留违规 (bugpattern-003)，
       │         历史上已修复过 2 次同类问题。"
       │
       └──→ AI 生成修复:
              HashMap → ConcurrentHashMap
              + 参考 fix_pattern 使用 computeIfAbsent
              + codegraph_impact 确认不破坏调用方
              + 补充并发单元测试
```

### 10.3 codekb_explain 增强

`codekb_explain` 返回结构增加 `known_issues` 字段：

```typescript
codekb_explain(symbol: string) → {
  summary: string
  knowledge: KnowledgeEntry[]
  known_issues: BugPatternEntry[]    // ← 新增：关联的缺陷模式
  callgraph: { callers: string[], callees: string[] }
  conventions: string[]
}
```

`known_issues` 仅包含 `related_symbols` 匹配当前符号的 bug-pattern 条目，按 severity 降序排列。AI 在修改该符号前即可了解"这里历史上出过什么问题"，避免重蹈覆辙或误改正确的防御性代码。

### 10.4 Hotfix 分支特殊处理

```
hotfix 分支工作流:

1. 创建 hotfix 分支
   → post-checkout 触发增量 sync（索引更新）
   → CodeKB 进入只读查询模式（不触发 extract）

2. AI 辅助定位和修复
   → codekb_search / codekb_explain / codekb_ask 正常使用
   → 不产生新的知识条目

3. 修复合入 main
   → main 上执行: codekb extract --from-change hotfix-xxx
   → 从 fix commit 中提取缺陷模式（Bug Pattern Extractor）
   → 若涉及已有 bug-pattern 条目 → 追加 source_commits
   → 若为新类型缺陷 → 创建新条目

4. 配置方式:
   # codekb.yaml
   branch_rules:
     hotfix:
       read_only: true          # 不触发提取
       extract_on_merge: true   # 合入 main 后提取
```

### 10.5 检索权重增强

`codekb_search` 的 Query Router 对 bugfix 相关查询做权重调整：

```
查询分类规则:
  包含 "bug" / "fix" / "error" / "异常" / "崩溃" / "丢失" / "死循环"
  → 判定为 bugfix 查询
  → bug-pattern 类型条目权重 × 2.0
  → decision 类型条目权重 × 1.5（理解设计意图有助于定位根因）
  → convention 类型条目权重 × 1.2（防御性编码建议）
```

---

## 十一、项目目录结构与配置

### 11.1 项目级结构

```
your-project/
├── openspec/                    # OpenSpec 规范（已有）
│   ├── specs/
│   └── changes/
├── .codegraph/                  # CodeGraph 索引（已有）
├── codekb/                      # CodeKB 知识库（新增）
│   ├── codekb.yaml              # 项目配置
│   ├── knowledge/               # 知识条目（Git 管理）
│   │   ├── architecture/
│   │   ├── decisions/
│   │   ├── rules/
│   │   ├── conventions/
│   │   └── bug-patterns/
│   └── index/                   # 本地索引（.gitignore）
│       ├── vectors.lance/
│       └── cache/
├── src/
└── ...
```

### 11.2 配置文件

```yaml
# codekb/codekb.yaml
version: 1

# 索引范围
include:
  - "src/**"
  - "lib/**"
  - "tests/**"
exclude:
  - "**/node_modules/**"
  - "**/dist/**"
  - "**/*.generated.*"

# 嵌入模型
embedding:
  provider: local              # local | voyage | openai-compatible
  model: jina-code-embeddings-0.5B
  # provider: voyage           # 云端模式
  # model: voyage-code-3
  # api_key_env: VOYAGE_API_KEY
  dimensions: 896
  batch_size: 32

# LLM 知识提取
extraction:
  provider: openai-compatible
  base_url: https://api.openai.com/v1   # 或任意兼容端点
  model: gpt-4o
  api_key_env: OPENAI_API_KEY
  extractors:
    - pattern                  # 架构模式识别
    - decision                 # 设计决策提取
    - rule                     # 业务规则提取
    - convention               # 编码约定提取
    - bug-pattern              # 缺陷模式提取
  auto_extract_on_archive: true  # OpenSpec archive 时自动提取

# 检索配置
retrieval:
  hybrid:
    structural_weight: 0.3
    semantic_weight: 0.5
    lexical_weight: 0.2
  reranker:
    enabled: false             # 可选开启
    model: jina-reranker-v2
  max_context_tokens: 4096

# 多仓库（大型/微服务场景）
# repos:
#   - path: ../user-service
#   - path: ../payment-service
#   - path: ../notification-service
```

---

## 十二、实施路线图

### Phase 1：核心检索能力（2-3 周）

目标：让 AI 能用自然语言搜索代码、获取代码解释。

- 实现 AST-aware 代码分块器（基于 tree-sitter）
- 集成 LanceDB，构建向量 + BM25 混合索引
- 集成 jina-code-embeddings-0.5B 本地嵌入推理
- 实现 MCP Server 基础工具：`codekb_search`、`codekb_explain`
- 实现增量索引（Git hook 触发）
- 与 CodeGraph MCP 联动（`codekb_explain` 内部调用 CodeGraph 获取调用关系）

交付物：可用的 MCP Server，AI 编码工具中配置后即可进行代码语义搜索。

### Phase 2：知识提取（2-3 周）

目标：自动从代码中提取结构化知识。

- 实现五类知识提取器（Pattern / Decision / Rule / Convention / Bug Pattern）
- 设计知识条目 Markdown 格式和存储结构（含 bug-patterns 目录）
- 实现 LLM 提取管线（支持 OpenAI-compatible API）
- 实现 `codekb_extract`、`codekb_knowledge`、`codekb_conventions` MCP 工具
- 实现提取缓存和增量提取
- 知识条目审阅机制（`codekb_review`）

交付物：首次索引后自动生成项目知识库（含缺陷模式），AI 编码时可引用设计决策、业务规则和历史 bug 模式。

### Phase 3：OpenSpec 集成 + 分支感知 + Bugfix 辅助（2-3 周）

目标：将知识库嵌入 OpenSpec 工作流，支持多分支协作和 bugfix 场景。

- 实现 `/opsx:explore` 阶段的知识概览注入
- 实现 `/opsx:propose` 阶段的约束关联
- 实现 spec 文件中的 `codekb:ref` 引用解析
- 实现 `/opsx:archive` 阶段的知识反哺
- 实现 `codekb_ask` RAG 问答工具
- 实现 post-checkout hook + 分支切换增量同步
- 实现知识条目分支感知（draft/accepted 生命周期）
- 实现 `codekb merge-resolve` 合并冲突处理
- 实现 `codekb_explain` 的 `known_issues` 字段
- 实现 bugfix 查询权重增强
- 实现 hotfix 分支只读模式

交付物：OpenSpec 工作流闭环 + 多分支协作支持 + bugfix 场景辅助。

### Phase 4：优化与扩展（持续）

- Cross-Encoder 重排序（提升检索精度）
- 多仓库联合索引（微服务场景）
- 知识图谱可视化（模块关系、知识关联）
- 支持更多 LLM 提取源（Jira/Confluence/Slack 等）
- 知识条目质量评估和自动去重

---

## 十三、技术选型总结

| 组件 | 选型 | 理由 |
|------|------|------|
| AST 解析 | tree-sitter（复用 CodeGraph） | 158 种语言、增量解析、已有基础设施 |
| 向量数据库 | LanceDB | 嵌入式无服务、原生混合检索（Tantivy BM25 + 向量）、Lance 列式存储 |
| 嵌入模型（本地） | jina-code-embeddings-0.5B | 开源 Apache 2.0、32K 上下文、代码检索 SOTA 级、可本地推理 |
| 嵌入模型（云端） | voyage-code-3 | 商业代码嵌入质量最佳、可变维度 |
| 知识提取 LLM | OpenAI-compatible API | 灵活切换任意兼容模型/端点 |
| MCP 实现 | TypeScript + @modelcontextprotocol/sdk | SDK 最成熟、与 OpenSpec npm 生态一致 |
| 知识存储 | Markdown + YAML（Git 管理） | 人类可编辑可审阅、版本可追溯、与 OpenSpec 格式一致 |
| 增量索引 | Git hook + Merkle Tree | 低开销、可靠、参照 Cursor 方案 |
| 重排序（可选） | jina-reranker-v2 | 开源、支持本地部署、多语言 |

---

## 附录 A：与主流方案对比

| 维度 | CodeKB（本方案） | Cursor 索引 | Sourcegraph Cody | Aider repo-map | codebase-memory-mcp |
|------|-----------------|------------|-----------------|----------------|---------------------|
| 部署 | 本地优先 + 可选云端 | 云端（代码上传） | 云端 SaaS | 纯本地 | 纯本地 |
| 结构检索 | 复用 CodeGraph | tree-sitter AST | SCIP/LSIF | tree-sitter tag | tree-sitter AST |
| 语义检索 | LanceDB 向量 + BM25 | 向量（Turbopuffer） | 向量 + 代码图 | 无 | 无 |
| 知识提取 | LLM 五类提取器（含缺陷模式） | 无 | 无 | 无 | 无 |
| 自然语言问答 | RAG（codekb_ask） | IDE 内问答 | 代码问答 | 无 | 无 |
| 规范集成 | OpenSpec 深度集成 | 无 | 无 | 无 | 无 |
| 代码隐私 | 代码不出本机 | 上传到云端 | 上传到云端 | 本地 | 本地 |
| 跨会话记忆 | 持久化知识条目 | 有限 | 有限 | 无 | 持久化图谱 |

## 附录 B：实施注意事项

**嵌入模型切换**：本地模型（jina-code-0.5B, 896维）和云端模型（voyage-code-3, 1024维）的向量维度不同，切换模型时需要全量重建向量索引。建议在 `codekb.yaml` 中记录当前使用的模型和维度，`codekb sync --reindex` 命令检测到配置变更时自动触发重建。

**tree-sitter 访问方式**：CodeKB 的代码分块需要直接访问 AST 子树（函数体、类定义等），而 CodeGraph 的 API 面向符号级查询。建议 CodeKB 直接调用 node-tree-sitter 进行分块解析，同时通过 CodeGraph MCP 获取调用关系等图查询结果——两条路径各司其职。

**CodeGraph 不可用时的降级**：若 `.codegraph/` 未初始化或索引过期，CodeKB 的结构化检索通道自动降级为仅使用语义 + 词法检索，`codekb_explain` 中的 callgraph 字段返回空并附带提示信息。首次运行时检测到 CodeGraph 缺失，应提示用户执行 `codegraph init -i`。

**低置信度知识条目**：`confidence < 0.7` 的知识条目在检索结果中标注为"待审阅"，不参与 `/opsx:propose` 阶段的自动约束关联（避免不准确的知识误导设计决策），但仍可通过 `codekb_knowledge` 显式查询。

**并发安全**：LanceDB 基于 MVCC 支持读写并发，Git hook 触发的增量索引写入不会阻塞 MCP Server 的查询读取。知识条目文件（Markdown）的写入采用原子写（写临时文件后 rename），避免读到半写状态。

**全量索引性能**：10 万行代码 5 分钟的目标在 CPU-only 环境下较为紧张（ONNX 批量嵌入约需 3-5 分钟）。建议首次索引时默认使用云端 Embedding API（若已配置），或提示用户预期等待时间。后续增量索引不受影响。

---

## 附录 C：关键参考

- OpenSpec: https://github.com/Fission-AI/OpenSpec — Spec-Driven Development 框架
- CodeGraph: tree-sitter 代码知识图谱 MCP Server
- LanceDB: https://lancedb.github.io/lancedb/ — 嵌入式向量数据库
- jina-code-embeddings: https://jina.ai/news/jina-code-embeddings-sota-code-retrieval-at-0-5b-and-1-5b/ — 开源代码嵌入模型
- voyage-code-3: https://docs.voyageai.com/docs/embeddings — 商业代码嵌入模型
- LSPRAG: https://arxiv.org/abs/2510.22210 — LSP 引导的代码 RAG 论文
- Aider repo-map: https://aider.chat/docs/repomap.html — 基于 tree-sitter 的仓库地图
- codebase-memory-mcp: 持久化代码知识图谱 MCP Server
