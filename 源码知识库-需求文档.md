# CodeKB 源码知识库 — 产品需求文档

> 版本：v1.0 | 日期：2026-08-02
> 所属框架：OpenSpec + CodeGraph + CodeKB AI Coding 体系

---

## 一、产品概述

### 1.1 背景

当前 AI Coding 框架由 OpenSpec（规范驱动开发）和 CodeGraph（代码结构图谱）两个组件构成。OpenSpec 管理"做什么"，CodeGraph 管理"代码结构是什么"，但两者均无法回答"为什么这样做"——架构决策的理由、隐含的业务规则、项目特有的编码约定等语义知识散落在 Git 历史、代码注释和团队成员的大脑中，AI 编码时无从获取。

### 1.2 产品定位

CodeKB 是框架的第三层能力——**语义知识层**。它从已有源码中自动提取结构化知识（架构模式、设计决策、业务规则、编码约定），并支持自然语言问答检索，使 AI 在编码时具备"项目记忆"，使新成员能快速理解项目的设计意图。

### 1.3 核心价值

对 AI 编码质量的提升：AI 不再仅凭代码结构猜测设计意图，而是基于明确的知识条目做决策，减少违反项目约定的代码生成（如该用 BigDecimal 的地方用了 double）。

对开发者效率的提升：新人入职不再需要花数周阅读代码和追问老员工"为什么这样设计"，通过自然语言问答即可获取带溯源的准确回答。

对知识资产的沉淀：设计决策和业务规则从"人脑中的隐性知识"变为"Git 管理的显性资产"，不因人员流动而丢失。

---

## 二、用户角色

### 2.1 AI 编码代理（主要用户）

通过 MCP 工具调用 CodeKB，在编码过程中获取知识上下文。不直接与 CLI 交互，而是通过 `codekb_search`、`codekb_ask`、`codekb_explain` 等 MCP 工具按需查询。核心诉求是获取准确、有界、分层的项目知识，避免生成违反项目约定的代码。

### 2.2 开发者（日常使用）

通过 AI 编码工具间接使用 CodeKB（AI 代理代为调用），也可直接通过 CLI 管理知识库。核心诉求是快速理解不熟悉的模块、确认设计约束、审阅和修正 AI 提取的知识条目。

### 2.3 技术负责人 / 架构师（知识管理）

关注知识库的整体质量，审阅关键设计决策和业务规则的准确性，维护编码约定。核心诉求是知识条目可编辑、可追溯、可治理。

---

## 三、功能需求

### F1：项目初始化与索引构建

**优先级：P0（Phase 1）**

**用户故事**：作为开发者，我希望在已有项目中通过一条命令完成 CodeKB 的初始化，使 AI 编码工具能立即理解我的项目。

**功能描述**：

F1.1 `codekb init` 命令在项目根目录执行，自动完成以下流程：探测项目语言和框架，生成 `codekb/codekb.yaml` 默认配置；检测 CodeGraph 是否已初始化（`.codegraph/` 目录），未初始化则提示用户先执行 `codegraph init -i`；对 include 范围内的代码执行全量 AST 分块和向量嵌入，构建 LanceDB 混合索引（向量 + BM25）；可选执行全量 LLM 知识提取（`--skip-extract` 跳过）。

F1.2 初始化完成后输出摘要报告：索引的文件数、代码行数、分块数、知识条目数（如执行了提取）、各类型知识分布、低置信度条目数量。

F1.3 自动生成 `.gitignore` 条目，将 `codekb/index/` 排除在版本管理外（索引可重建，不需入库），`codekb/knowledge/` 保留在版本管理中。

F1.4 输出 MCP Server 配置片段，提示用户添加到 AI 编码工具的 MCP 配置中。

**验收标准**：

- 在一个 5 万行 TypeScript 项目上执行 `codekb init`，全流程（含本地嵌入）在 3 分钟内完成
- 生成的 `codekb.yaml` 正确识别项目语言，include/exclude 规则合理
- `codekb/index/vectors.lance/` 目录生成，包含正确的向量索引
- 若 CodeGraph 未初始化，给出明确提示而非报错崩溃
- 重复执行 `codekb init` 不破坏已有知识库（幂等），仅重建索引

---

### F2：代码语义检索

**优先级：P0（Phase 1）**

**用户故事**：作为 AI 编码代理，我希望能用自然语言搜索项目代码，找到与当前任务相关的代码片段和模块。

**功能描述**：

F2.1 `codekb_search` MCP 工具接受自然语言查询，执行三路混合检索（结构化 CodeGraph + 向量语义 + BM25 词法），通过 RRF 融合排序，返回 Top-K 结果。

F2.2 支持 `scope` 参数限定搜索范围（目录/模块），支持 `knowledge_types` 参数过滤知识类型。

F2.3 每条搜索结果包含：代码片段或知识条目内容、来源文件路径和行号、相关性分数、匹配通道标注（structural / semantic / lexical）。

F2.4 查询路由逻辑：精确符号名查询优先走 CodeGraph 结构化检索；自然语言描述优先走向量语义检索；包含错误信息或字符串字面量的查询优先走 BM25。

**验收标准**：

- 查询 "哪里处理了用户鉴权逻辑" 能在 Top-5 中返回 auth 相关模块
- 查询 "PaymentService.processRefund" 能在 Top-1 返回该符号的定义
- 查询 "NullPointerException OrderService" 能通过 BM25 精确命中包含该字符串的代码
- 10 万条索引下，混合检索端到端延迟 < 200ms
- 结果中包含来源文件路径，可被 AI 直接用于后续 Read 操作

---

### F3：符号解释

**优先级：P0（Phase 1）**

**用户故事**：作为 AI 编码代理，当我需要修改一个函数时，我希望一次性获取它的功能摘要、关联的设计约束和调用关系。

**功能描述**：

F3.1 `codekb_explain` MCP 工具接受符号名，返回聚合信息：LLM 生成的功能摘要、关联的知识条目（设计决策、业务规则）、调用关系（复用 CodeGraph 的 callers/callees）、涉及的编码约定。

F3.2 若符号无知识标注，触发按需实时提取（调用云端 LLM），结果缓存到 `codekb/index/cache/`，不直接写入 `knowledge/` 目录。

F3.3 若 CodeGraph 不可用，callgraph 字段返回空并附带降级提示，其余字段正常返回。

**验收标准**：

- `codekb_explain("PaymentService.processRefund")` 返回包含 summary、knowledge、callgraph、conventions 四个字段的完整结果
- 对无知识标注的符号，首次调用触发实时提取（< 10s），后续调用命中缓存（< 100ms）
- CodeGraph 未初始化时不报错，callgraph 返回 `{ "callers": [], "callees": [], "_note": "CodeGraph not initialized" }`

---

### F4：知识提取

**优先级：P0（Phase 2）**

**用户故事**：作为技术负责人，我希望 CodeKB 能自动从代码中提取设计决策和业务规则，减少人工整理知识的工作量。

**功能描述**：

F4.1 五类知识提取器，通过 LLM 从多种来源提取结构化知识：

- 架构模式识别器（Pattern Detector）：分析模块依赖图和通信方式，识别事件驱动、CQRS、Saga、分层等架构模式
- 设计决策提取器（Decision Extractor）：从 Git commit message、PR 描述、`// WHY:` / `// NOTE:` / `// HACK:` 注释中提取决策及理由，输出轻量级 ADR 格式
- 业务规则提取器（Rule Extractor）：从条件分支、校验逻辑、常量定义、测试用例（given/when/then）中提取隐含业务规则
- 编码约定提取器（Convention Extractor）：从代码风格一致性中推断命名规范、错误处理模式、日志规范等，结合 `.editorconfig`、lint 配置交叉验证
- 缺陷模式提取器（Bug Pattern Extractor）：从 Git 历史中的 fix/bugfix/hotfix commit 及关联的 issue/PR 描述中提取缺陷模式，包括触发条件、修复模式、涉及符号、防御性编码建议

F4.2 `codekb extract` CLI 命令和 `codekb_extract` MCP 工具触发提取，支持参数：`--scope`（限定范围）、`--extractors`（选择提取器）、`--force`（忽略缓存重新提取）、`--from-change <name>`（从 OpenSpec 变更中提取）。

F4.3 提取结果以 Markdown + YAML frontmatter 格式输出到 `codekb/knowledge/` 对应子目录，每条知识包含：id、type、title、status、date、related_symbols、tags、confidence、source 字段。缺陷模式条目额外包含：severity（high/medium/low）、trigger_conditions（触发条件列表）、fix_pattern（修复模式）、source_commits（来源 commit 列表）。

F4.4 提取缓存机制：对每个输入源计算内容哈希，未变更的源不重复调用 LLM。缓存存储在 `codekb/index/cache/`。

F4.5 提取完成后输出报告：新增条目数、更新条目数、各类型分布、平均置信度、需审阅条目列表（confidence < 0.7）。

**验收标准**：

- 在一个包含 20 个服务类的项目上执行全量提取，能识别出至少 3 种架构模式、5 条设计决策、3 条业务规则
- 对包含 10+ 个 fix commit 的项目，Bug Pattern Extractor 能提取出至少 2 条缺陷模式，每条包含 trigger_conditions 和 fix_pattern
- 知识条目的 related_symbols 字段正确关联到 CodeGraph 中的符号
- 对同一范围重复执行提取（无代码变更），不产生 LLM 调用（命中缓存）
- 提取的 Markdown 文件格式合法，YAML frontmatter 可被标准解析器解析
- confidence < 0.7 的条目在报告中明确标注为"待审阅"

---

### F5：知识条目管理

**优先级：P1（Phase 2）**

**用户故事**：作为技术负责人，我希望能审阅、修正、确认 AI 提取的知识条目，确保知识库的准确性。

**功能描述**：

F5.1 `codekb_review` MCP 工具和 `codekb review` CLI 命令支持对知识条目的三种操作：confirm（确认准确，提升 confidence 到 1.0）、reject（标记为不准确，status 改为 rejected，不再参与检索）、edit（修改内容，保留修改历史）。

F5.2 `codekb_knowledge` MCP 工具和 `codekb list` CLI 命令支持按 type、tags、related_symbol、status 过滤浏览知识条目。

F5.3 知识条目支持 `supersedes` / `superseded_by` 字段，标记决策的取代关系。被取代的条目 status 自动改为 `superseded`，检索时默认不返回。

F5.4 所有知识条目变更通过 Git 版本管理追踪，`git log -- codekb/knowledge/` 可查看完整变更历史。

**验收标准**：

- `codekb review decision-001 confirm` 后，该条目 confidence 变为 1.0
- `codekb review decision-003 reject` 后，`codekb_search` 不再返回该条目
- 被 supersede 的旧条目不出现在默认检索结果中，但 `codekb list --include-superseded` 可查看
- 知识条目的每次修改在 Git 历史中有独立 commit 记录

---

### F6：自然语言问答

**优先级：P1（Phase 3）**

**用户故事**：作为开发者，我希望能用自然语言提问关于项目的问题，得到基于实际代码的准确回答，而不是泛泛的通用回答。

**功能描述**：

F6.1 `codekb_ask` MCP 工具接受自然语言问题，执行 RAG 流程：检索相关代码片段和知识条目 → 组装分层上下文（知识层 + 结构层 + 代码层，总计 ≤ 4K tokens）→ 调用 LLM 生成回答。

F6.2 回答附带 `sources` 字段，列出引用的文件路径、知识条目 ID、符号名，支持溯源验证。

F6.3 支持 `context_symbols` 参数，传入当前正在编辑的符号作为额外上下文，提升回答的针对性。

F6.4 当知识库中无相关信息时，明确回答"项目中未找到相关知识"，而非编造回答。

**验收标准**：

- 提问 "退款失败时系统怎么处理的" 返回基于实际代码的回答，引用具体的错误处理函数和重试逻辑
- 提问 "为什么消息模块用事件驱动" 返回包含设计理由的回答，引用对应的 decision 知识条目
- 回答的 sources 字段包含至少一个可验证的文件路径或知识条目 ID
- 对项目中不存在的话题（如 "Kubernetes 部署配置是什么" 而项目无 K8s），返回"未找到"而非编造

---

### F7：编码约定查询

**优先级：P1（Phase 2）**

**用户故事**：作为 AI 编码代理，在生成代码前我希望获取项目或特定模块的编码约定，确保生成的代码符合项目风格。

**功能描述**：

F7.1 `codekb_conventions` MCP 工具返回全局或特定领域（domain 参数）的编码约定列表。

F7.2 每条约定包含：约定内容描述、适用范围（全局 / 特定模块）、来源（lint 配置 / 代码推断 / 人工录入）、示例代码片段。

F7.3 支持与 `.editorconfig`、ESLint/Prettier 配置、CI 检查规则的交叉验证——如果 lint 配置与代码推断的约定矛盾，以 lint 配置为准并标注。

**验收标准**：

- `codekb_conventions()` 返回项目全局约定（如错误处理模式、日志规范）
- `codekb_conventions("payment")` 返回支付模块特有约定（如 BigDecimal 要求）
- 约定条目中包含来源标注，区分"lint 配置确认"和"代码风格推断"

---

### F8：OpenSpec 工作流集成

**优先级：P1（Phase 3）**

**用户故事**：作为开发者，我希望在 OpenSpec 的各个阶段自动获得 CodeKB 的知识支持，不需要手动查询。

**功能描述**：

F8.1 `/opsx:explore` 阶段集成：AI 执行 explore 时，CodeKB 自动提供项目知识概览（架构模式列表、核心业务规则数量、关键设计决策摘要、编码约定要点），作为探索的起点。

F8.2 `/opsx:propose` 阶段集成：AI 撰写变更提案时，CodeKB 根据提案涉及的模块自动检索相关约束（设计决策 + 业务规则），附加到提案中。仅关联 confidence ≥ 0.7 的条目。

F8.3 Spec 文件知识引用：在 OpenSpec 的 spec 文件中支持 `<!-- codekb:ref <id> -->` 注释语法引用知识条目。`/opsx:propose` 阶段自动解析这些引用，确保变更提案不违反已有约束。

F8.4 `/opsx:apply` 阶段集成：AI 编码时通过 MCP 工具按需查询知识（F2/F3/F6/F7），无额外自动化行为。

F8.5 `/opsx:archive` 阶段集成（知识反哺）：变更归档后自动触发知识提取（`codekb extract --from-change <name>`），从 proposal.md、spec-delta.md、archive.md 及涉及的代码文件中提取新知识。新条目的 `source_change` 字段溯源到 OpenSpec 变更名。若新知识取代了旧决策，自动更新旧条目的 status 为 `superseded`。

F8.6 配置项 `auto_extract_on_archive: true/false` 控制 archive 阶段是否自动触发提取，默认 true。

**验收标准**：

- `/opsx:explore` 执行时，AI 的输出中包含项目知识概览信息
- `/opsx:propose` 生成的提案中包含 "相关约束" 部分，引用了正确的知识条目 ID
- spec 文件中的 `codekb:ref` 引用在 propose 阶段被正确解析
- `/opsx:archive` 后 `codekb/knowledge/` 目录中出现新的知识条目，`source_change` 字段正确
- 设置 `auto_extract_on_archive: false` 后，archive 不触发自动提取

---

### F9：增量索引与同步

**优先级：P0（Phase 1）**

**用户故事**：作为开发者，我希望代码变更后 CodeKB 的索引自动更新，不需要手动重建。

**功能描述**：

F9.1 `codekb init` 时自动安装 Git post-commit hook，每次 commit 后触发 `codekb sync --incremental`。

F9.2 增量同步流程：通过 `git diff` 获取变更文件列表 → tree-sitter 增量解析 → 重新分块和嵌入变更的代码 → upsert LanceDB 向量索引 → 更新 BM25 索引 → 检查变更符号是否关联已有知识条目，若关联则标记为 `potentially_stale`。

F9.3 代码索引（向量 + BM25）自动实时更新；知识条目仅标记过期，不自动改写（避免 LLM 误判）。

F9.4 `codekb sync` CLI 命令支持手动触发，`--reindex` 参数强制全量重建索引（嵌入模型变更时使用）。

F9.5 Merkle Tree 校验：对每个文件计算内容哈希构建 Merkle Tree，通过根哈希快速判断索引是否需要更新，避免不必要的重解析。

F9.6 `codekb status` CLI 命令显示索引健康状态：上次同步时间、待同步文件数、索引覆盖率、过期知识条目数。

**验收标准**：

- 修改一个文件并 commit 后，增量同步在 5 秒内完成
- 同步后 `codekb_search` 能检索到新修改的代码内容
- 变更涉及的已有知识条目被标记为 `potentially_stale`
- `codekb status` 正确显示待同步文件数和过期条目数
- 删除 `.codegraph/` 后执行 `codekb sync`，结构化通道降级但语义和词法通道正常

---

### F10：多仓库支持

**优先级：P2（Phase 4）**

**用户故事**：作为微服务架构的开发者，我希望 CodeKB 能跨多个仓库联合检索，理解服务间的关系。

**功能描述**：

F10.1 全局配置文件 `~/.codekb/config.yaml` 支持注册多个仓库路径。

F10.2 每个仓库独立维护 LanceDB 索引和知识条目，`codekb_search` 支持跨仓库联合检索。

F10.3 跨仓库结果标注来源仓库，支持按仓库过滤。

**验收标准**：

- 注册 3 个微服务仓库后，`codekb_search` 能返回跨仓库的结果
- 每条结果标注来源仓库名
- 单仓库索引更新不影响其他仓库

---

### F11：多分支支持

**优先级：P1（Phase 3）**

**用户故事**：作为团队开发者，我在多个 feature 分支间频繁切换，希望 CodeKB 的索引和知识在分支切换后自动保持一致，不产生过期或错误的检索结果。

**功能描述**：

F11.1 `codekb init` 时同时安装 post-checkout Git hook，分支切换后自动触发 `codekb sync --incremental`，通过 Merkle Tree 差异检测仅重新处理两分支间变更的文件。

F11.2 知识条目增加分支感知字段：`branch_origin`（产生该条目的分支）、`status: draft`（feature 分支上产生的条目默认为 draft，仅在当前分支参与检索）、`promoted_to_main: boolean`（合入 main 后标记为 true，status 提升为 accepted）。

F11.3 分支类型策略（可在 `codekb.yaml` 中配置）：

- main/master：完整索引，知识条目为权威版本（status=accepted）
- feature/*：增量索引，新产生的知识条目 status=draft，仅本分支可见
- hotfix/*：增量索引，只读查询模式（不触发知识提取），修复合入 main 后沉淀知识（详见 F12.4）
- release/*：索引只读，不触发自动提取

F11.4 `codekb merge-resolve` CLI 命令：在 git merge 产生 `codekb/knowledge/` 下的冲突时调用，列出冲突条目，对语义冲突（两条决策矛盾）调用 LLM 生成合并建议，对简单冲突（不同位置新增）自动合并。

F11.5 检索时的分支过滤规则：默认仅返回当前分支可见的条目（main 上的 accepted 条目 + 当前分支的 draft 条目）。`codekb_search` 和 `codekb_knowledge` 支持 `include_drafts: false` 参数排除 draft 条目。

F11.6 LanceDB 索引分支策略：默认使用单索引 + 增量更新（切换分支时 sync）；大型项目（50 万行+）且频繁切换分支时，可配置为多索引快照模式（每个活跃分支维护独立索引表，切换时零延迟）。

**验收标准**：

- 从 main 切换到 feature 分支后，增量同步在 3 秒内完成（30 个文件差异）
- feature 分支上 extract 产生的条目 status 为 draft，切回 main 后 `codekb_search` 不返回该条目
- feature 合入 main 后，原 draft 条目 status 自动提升为 accepted
- 两个分支各自新增知识条目后 merge，`codekb merge-resolve` 能正确处理冲突
- release 分支上不触发自动提取

---

### F12：Bugfix 辅助

**优先级：P1（Phase 3）**

**用户故事**：作为开发者，在修复 bug 时我希望 AI 能利用知识库中的设计意图、缺陷模式和影响分析，快速定位根因并生成不引入新问题的修复。

**功能描述**：

F12.1 Bug Pattern 检索增强：`codekb_search` 对包含"bug"、"fix"、"error"、"异常"等关键词的查询，自动提升 bug-pattern 类型条目的权重。

F12.2 `codekb_explain` 返回结果增加 `known_issues` 字段：列出该符号关联的缺陷模式条目（bug-pattern 类型），帮助 AI 在修改前了解历史问题。

F12.3 Bugfix 工作流集成：AI 在 bugfix 场景中可通过 `codekb_ask` 获取"这个模块的设计意图是什么"（对比当前代码是否违反意图）、"类似问题的历史修复方式"（检索 bug-pattern）、"修改这里会影响什么"（结合 CodeGraph impact）。

F12.4 Hotfix 分支特殊处理：hotfix 分支上 CodeKB 以只读查询模式工作（不触发知识提取），修复合入 main 后在 main 上执行 `codekb extract --from-change hotfix-xxx` 将修复经验沉淀为知识。

F12.5 缺陷模式条目自动关联：当新的 fix commit 涉及的符号与已有 bug-pattern 条目相关时，自动在该条目中追加 `source_commits`，形成"同一类问题的修复历史"。

**验收标准**：

- 搜索 "并发 数据丢失" 能在 Top-3 中返回相关的 bug-pattern 条目
- `codekb_explain("SessionCache")` 返回的 known_issues 字段包含关联的缺陷模式
- hotfix 分支上不产生新的知识提取调用
- 同一符号的多次 fix commit 被聚合到同一条 bug-pattern 中

---

## 四、非功能需求

### N1：代码隐私

核心索引（AST 图谱、向量索引、BM25 索引）全部在本地构建和存储，代码原文不上传到任何外部服务。嵌入推理默认使用本地模型（jina-code-0.5B），仅在用户显式配置后才使用云端 Embedding API。知识提取和问答生成调用云端 LLM 时，发送的是代码片段（非完整仓库），且用户可配置为使用本地 LLM 端点。

### N2：性能

| 操作 | 目标 |
|------|------|
| 结构化查询 | < 5ms |
| BM25 检索 | < 50ms |
| 向量 ANN 检索（10 万条） | < 100ms |
| 混合检索 + 融合 | < 200ms |
| 增量同步（单次 commit） | < 5s |
| 全量索引（10 万行，本地嵌入） | < 5min |
| LLM 知识提取（单符号） | < 10s |

### N3：可扩展性

支持 1 万行到 50 万行以上的代码库。50 万行以上通过模块级懒加载和分仓库索引适配。支持 tree-sitter 覆盖的所有语言（150+ 种）。

### N4：可维护性

知识条目以 Markdown + YAML 存储，人类可直接编辑，Git 版本管理追踪变更。索引可从源码完全重建（`codekb sync --reindex`），不依赖外部状态。配置文件（`codekb.yaml`）自描述，包含注释说明每个选项。

### N5：兼容性

MCP Server 兼容所有支持 MCP 协议的 AI 编码工具（Claude Code、Cursor、Windsurf、Copilot、Aider 等）。CLI 支持 macOS 和 Linux。Node.js ≥ 18。

---

## 五、CLI 命令总览

```
codekb init [--skip-extract]          # 初始化项目
codekb sync [--incremental|--reindex] # 同步索引
codekb extract [--scope <path>]       # 触发知识提取
             [--extractors <list>]
             [--from-change <name>]
             [--stale]
             [--force]
codekb list [--type <type>]           # 浏览知识条目
            [--tags <tags>]
            [--status <status>]
            [--include-superseded]
            [--include-drafts]
codekb review <id> <confirm|reject|edit>  # 审阅知识条目
codekb merge-resolve                  # 合并冲突知识条目
codekb status                         # 索引健康状态
codekb mcp                            # 启动 MCP Server
```

---

## 六、MCP 工具总览

```
codekb_search(query, options?)        # 混合检索
codekb_ask(question, options?)        # RAG 问答
codekb_explain(symbol)                # 符号解释
codekb_conventions(domain?)           # 编码约定
codekb_knowledge(filter?)             # 知识条目浏览
codekb_extract(scope, options?)       # 触发知识提取
codekb_review(id, verdict, edit?)     # 知识条目审阅
```

---

## 七、依赖关系

### 7.1 硬依赖

CodeGraph MCP Server：CodeKB 的结构化检索通道复用 CodeGraph 的符号图谱。CodeGraph 不可用时降级为语义 + 词法双通道检索。

tree-sitter：代码分块和 AST 解析的基础。通过 node-tree-sitter 直接调用。

### 7.2 软依赖

OpenSpec：F8（工作流集成）依赖 OpenSpec 的 `/opsx:*` 命令流程。OpenSpec 未安装时 F1-F7、F9-F10 独立可用。

云端 LLM API：知识提取（F4）和问答（F6）需要 LLM。可配置为本地端点（如 Ollama），但提取质量会下降。

### 7.3 可选依赖

jina-reranker-v2：Cross-Encoder 重排序，提升检索精度。未安装时跳过重排序阶段。

voyage-code-3 API：云端嵌入模型，替代本地 jina-code-0.5B 以获得最佳检索质量。

---

## 八、交付计划

| 阶段 | 功能范围 | 周期 | 交付物 |
|------|---------|------|--------|
| Phase 1 | F1 初始化 + F2 语义检索 + F3 符号解释 + F9 增量索引 | 2-3 周 | 可用的 MCP Server + CLI |
| Phase 2 | F4 知识提取（含 Bug Pattern）+ F5 知识管理 + F7 编码约定 | 2-3 周 | 完整知识库生成能力 |
| Phase 3 | F6 自然语言问答 + F8 OpenSpec 集成 + F11 多分支 + F12 Bugfix 辅助 | 2-3 周 | 工作流闭环 + 分支感知 |
| Phase 4 | F10 多仓库 + 重排序 + 可视化 | 持续 | 企业级扩展 |

---

## 九、开放问题

OQ1：知识提取的 LLM 成本如何控制？全量提取一个 50 万行项目可能产生大量 API 调用。是否需要引入本地小模型做初筛，仅对高价值模块调用大模型？

OQ2：多人协作场景下，不同开发者对同一知识条目的 review 结论冲突如何处理？是否需要引入"知识管理员"角色做最终裁决？

OQ3：知识条目的粒度如何把握？过粗（"支付模块很复杂"）没有指导价值，过细（"第 47 行用了 BigDecimal"）噪声太大。当前设计以"一个设计决策 / 一条业务规则 / 一个约定"为粒度单位，是否需要在实践中调整？

OQ4：是否需要支持从外部知识源（Confluence、Notion、Jira）导入知识？这些来源的知识与代码提取的知识如何融合和去重？
